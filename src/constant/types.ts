export type NavLinkType = {
  title: string;
  url: string;
}[];

export type HeroSectionType = {
  headline: string;
  shortHeadline: string;
};

export type EssentialType = {
  headline: string;
  shortHeadline: string;
  buttonText: string;
  img_Url: string;
};
